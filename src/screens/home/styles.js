import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  list: {
      backgroundColor: 'red',
      width: '100%',
      height: '100%',
  }
});

export default styles;